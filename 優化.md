- 目前畫面資訊過多，模糊了使用者焦點，將不必要的資訊改為折疊面板
- 目前把影片跟畫面的預覽關閉了，要添加回來但是改為預設不開啟，只有使用者額外點選時才會開啟
- 目前使用 mac 輸出的 mov 檔案會無法讀取
- 預期加上版本號控制，並且在 readme.md 中說明如何修改
    - 同步需要撰寫 changelog.md，並且在 readme.md 中說明如何修改
    - 預期要透過 github action 偵測到有 tag 產生時，自動使用 changelog.md 產生 release note，並且自動發布 github release

## 盤點結果

盤點基準：2026-08-11，`HEAD ecfe64a`。本專案將「驗收完成」定義為同時滿足：程式碼已實作、相關驗證已通過、文件與追蹤紀錄同步、且已納入指定交付分支。以下「已實作」不等於「驗收完成」。

| 項目 | 程式碼 | 驗證 | 文件／追蹤 | 交付分支 | 判定 |
| --- | --- | --- | --- | --- | --- |
| 資訊改為折疊面板 | 已實作批次進階設定、單檔覆寫與螢幕錄影比較面板的預設收合 | lint/build 通過；Docker Chromium UI/E2E 通過 | ADR-0001、`tests/e2e/optimization.spec.mjs` 已記錄 | 原始折疊功能已在 `main`，目前比較面板變更尚未納入 `main` | 已實作，驗收證據已建立；待交付分支同步 |
| 預設關閉、按需開啟影片與圖片預覽 | 已實作原始與壓縮後預覽，點擊後才建立 Blob URL | lint/build 通過；Docker Chromium 驗證預覽 demand-loaded | ADR-0002、`tests/e2e/optimization.spec.mjs` 與 CHANGELOG 已記錄 | 已在 `main` | 已實作，驗收證據已建立 |
| 支援 macOS MOV | 已加入 QuickTime/MOV 解析；H.264/AAC MOV fixture 可讀取 metadata；不支援的 codec/encoder 會顯示錯誤 | Docker Chromium 讀取與移除音訊後輸出 MP4 的 E2E 通過；Alpine Chromium 不支援目前 AAC WebCodecs encoder 組合，保留音訊路徑待支援環境驗證 | ADR-0003、ADR-0010、`docs/verification/optimization-acceptance-2026-08-11.md` 與 CHANGELOG 已記錄 | 已在 `main` | 已實作，部分驗收完成；AAC 保留音訊待環境驗證 |
| SemVer、CHANGELOG 與 tag 自動 Release | package 版本比對、CHANGELOG 擷取及 tag Release workflow 已實作 | lint/build 通過；本輪未實際推送 tag 觸發 GitHub Release | ADR-0004～0006、README、CHANGELOG 已記錄；相關 #4～#8 仍 OPEN | 原始流程已在 `main`；目前 PWA-only release 變更尚未納入 `main` | 已實作，待發布驗證 |

### 追蹤狀態

- GitHub [#1](https://github.com/ben870217/media-compressor/issues/1)～[#3](https://github.com/ben870217/media-compressor/issues/3)、[#4](https://github.com/ben870217/media-compressor/issues/4)～[#8](https://github.com/ben870217/media-compressor/issues/8)、[#9](https://github.com/ben870217/media-compressor/issues/9)～[#15](https://github.com/ben870217/media-compressor/issues/15) 目前皆為 `OPEN`。
- `.scratch/screen-recording-compression/issues/01–03` 已改標示 `implemented-not-accepted`；實作 checklist 已依程式碼與測試證據同步，但 GitHub issue 仍 OPEN，因此不能標為「驗收完成」。
- 本地單元測試 31/31、Docker Chromium UI/E2E 5/5 通過；ESLint 與 production build 通過。驗證細節與 AAC encoder 限制見 [`docs/verification/optimization-acceptance-2026-08-11.md`](docs/verification/optimization-acceptance-2026-08-11.md)。
