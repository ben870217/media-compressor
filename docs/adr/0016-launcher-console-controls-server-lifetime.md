# ADR-0016：以啟動器命令視窗控制本機伺服器生命週期

## 狀態

已接受

## 背景

離線啟動器必須在 Chrome 使用期間保持 localhost 伺服器運作，同時讓使用者可明確停止它。第一版不需要系統匣或額外桌面 UI。

## 決策

啟動器開啟 Chrome 後，保留命令視窗並顯示「使用完畢請關閉此視窗」。使用者關閉該視窗時，啟動器停止 localhost 伺服器並結束。

## 後果

- 使用者能直接理解如何結束離線服務。
- 不需實作系統匣圖示或額外控制介面。
- 使用者在使用期間會看到一個命令視窗；README 必須解釋此為預期行為。
