# ADR-0011：進階設定僅在目前頁面與批次內保存

## 狀態

已接受

## 背景

折疊進階設定需要保留使用者在當前工作流程中選擇的值，但跨工作階段保存可能讓舊設定在未察覺下影響新的媒體。

## 決策

進階設定與其摘要只在目前頁面及同一批次中保存。重新整理頁面或開啟新分頁後，一律使用預設值；不新增 `localStorage` 或其他持久化偏好設定。

## 後果

- 同批次操作時，設定不會因面板收合而遺失。
- 新工作階段有可預期的乾淨預設值。
- 使用者無法跨工作階段沿用偏好；若日後需要，必須另行設計明確的偏好管理介面。
