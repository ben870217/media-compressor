# ADR-0020：下載保留原始超標輸出與單次重試輸出

## 狀態

已接受

## 背景

超標輸出與自動重試輸出都需保留供使用者比較；若沿用相同檔名，瀏覽器下載時難以辨識或可能造成衝突。

## 決策

- 原任務與「自動重試 1/1」任務的成功輸出皆可個別下載。
- 「全部依序下載」包含兩個結果。
- 重試輸出檔名追加 `_retry-1`，原任務保留既有的 `_compressed` 命名。

## 後果

- 使用者可離線比較兩次輸出。
- 一個來源檔可能產生兩個下載檔案，介面須清楚標示其差異。
