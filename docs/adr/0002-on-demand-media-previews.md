# ADR-0002：媒體預覽採按需載入

## 狀態

已接受

## 背景

影片與圖片的壓縮前後預覽曾被關閉，但使用者仍需要在必要時檢視與比較媒體。自動載入預覽會增加大型檔案的記憶體與解碼負擔。

## 決策

影片與圖片的壓縮前與壓縮後預覽皆預設不載入。每一側各提供獨立的「顯示預覽」控制；只有使用者操作後才建立 Blob URL 並載入媒體。收合預覽或切換檔案時，必須釋放對應 Blob URL。

## 後果

- 初始畫面與壓縮流程不會因預覽占用不必要的資源。
- 使用者能只開啟需要檢視的一側。
- 元件必須妥善管理 Blob URL 的建立與釋放生命週期。
